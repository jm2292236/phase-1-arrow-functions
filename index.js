// Function expression
const divide = function () {return 2000 / 100};

// Arrow function with only one parameter
// No parenthesis needed to enclose the parameter
const square = number => number * number;

// Arrow function with more than one paramenter
// Parenthesis and comma are necessary for the parameters
const add = (number1, number2) => number1 + number2;
